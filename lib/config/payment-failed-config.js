'use strict';

// eslint-disable-next-line no-unused-vars
const config = {
    card:{
        status: 'オーソリNG',
        template: 'NPNGメール',
    },
    np:{
        status:2,
        template:3,
    },
}

module.exports = config;
